---
title: "File upload"
description: "Upload CSVs and reference data with @file/column."
icon: "file-arrow-up"
---

Upload a CSV during generation to enable column references in prompts:

```text
use @users.csv/email and @users.csv/password to log in
```

After upload, select columns via autocomplete and include them in steps or variables.

